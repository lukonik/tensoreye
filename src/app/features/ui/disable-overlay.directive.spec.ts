import { DisableOverlayDirective } from './disable-overlay.directive';

describe('DisableOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
